var buf = new Buffer('Hello', 'utf8');
// take this string 'Hello' and convert it to binary data. To convert it, use the 'utf8' encoding.
console.log(buf);
console.log(buf.toString());
console.log(buf.toJSON());
